# .gitattributes — WaitlistKit
# Generated by /edbot-harness:harness-init on 2026-06-21.

# Roadmap is generated from backlog frontmatter — merge strategy: union to surface conflicts, not hide them.
# (In remote/GitHub-Issues mode this is informational — the tracker is the source of truth.)
docs/roadmap.md         merge=union

# Prisma migrations are append-only — union merge surfaces conflicts without silent clobber.
prisma/migrations/      merge=union

# Lock files — union merge to avoid silent drops (resolve manually after).
package-lock.json       merge=union
pnpm-lock.yaml          merge=union
yarn.lock               merge=union

# Generated files — treat as binary to prevent diff noise.
.next/                  binary
